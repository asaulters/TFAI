{
  "name": "taskflowai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/app.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
